var name;
var pass;

name = prompt ("Enter name");
pass = prompt ("Enter password");

if (name == "ivan" && pass == 333 || name == "ssss" && pass == 666 || name == "gibs" && pass == 0000) {
	alert("Welcome");
	} else {
	alert("Usert not found")
	}